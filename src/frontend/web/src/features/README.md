# `features`

Page and feature **composition** for guided user workflows.

- Compose primitives + read-models into pages/sections
- May coordinate multiple domains at the UX layer via application/API contracts
- Must **not** encode “one backend domain = one menu/screen tree”
- Prefer workflow/use-case oriented feature folders when implementation begins
