# Experience Tour Detail fixtures (UIVAL-T003)

Typed `TourDetailPageViewModel` with `kind: "Experience"` for dev validation route.

| File | Role |
|------|------|
| `fa.ts` / `en.ts` | Locale-specific published fixtures |
| `index.ts` | `loadExperienceTourDetailFixture(locale)` |

Route: `/[locale]/dev/experience-tour` → `TourDetailView` + `ExperienceTourDetailSections`.

Not live Tour API data.
