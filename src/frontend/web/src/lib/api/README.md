# `lib/api`

Frontend **application / API access boundary**.

- Typed ports for page read-models and adapters belong here later
- No database, EF, connection strings, or module DbContext imports
- OpenAPI codegen is not required for early P02 (plan default)
