<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>TravelCore.Tools.DemoFeed</RootNamespace>
    <AssemblyName>TravelCore.Tools.DemoFeed</AssemblyName>
    <!-- Temporary DEMOFEED tool — not a product module. Removable after GATE purge. -->
    <Description>Temporary removable DEMOFEED feeder host/boundary (TC-DEMOFEED-T002).</Description>
  </PropertyGroup>

  <!--
    T002 intentionally has ZERO project references to Modules/* or TravelCore.Api.
    Later seed tasks (T003+) may add Contracts-only / controlled host wiring —
    never ITravelCoreModule registration into production Program.cs.
  -->

</Project>
